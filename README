libbmp library to manipulate 24bpp or more BMPs.
Includes converting to grayscale, sepia, negative.
Includes rotation, mirroring, border addition and deletion.

makefile included.
Run setup.sh to create the directory structure. The script also invokes make to build.
